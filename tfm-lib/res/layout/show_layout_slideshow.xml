<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
	xmlns:android="http://schemas.android.com/apk/res/android"
	android:layout_width="fill_parent"
	android:layout_height="fill_parent"
>
	<!--  use this layout when you need native animation -->
	<com.hancom.show.animation.SlideShowView
		android:id="@id/show_drawable_native_slide_view"
		android:background="#00000000"
		android:layout_width="fill_parent"
		android:layout_height="fill_parent"
		android:visibility="gone"
	/>
	
	<!-- slide show menu button -->
	<ImageButton 
		android:id="@id/show_slideshow_menu_button"
		android:src="@drawable/show_selector_slideshow_menu_button"
		android:layout_width="wrap_content" android:layout_height="wrap_content" 
		android:layout_gravity="right|bottom" android:background="@android:color/transparent" />
</FrameLayout>